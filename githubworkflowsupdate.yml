name: Update data from Excel

on:
  push:
    paths:
      - 'Книга1.xlsx'
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

# ⬇️ 1. ДОБАВЛЯЕМ РАЗРЕШЕНИЯ НА ЗАПИСЬ ⬇️
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install pandas openpyxl

      - name: Run parser
        run: python parse_excel.py

      - name: Commit and push data.json
        run: |
          # ⬇️ 2. НАСТРАИВАЕМ GIT (ДОБАВЛЯЕМ ЭТИ ДВЕ СТРОЧКИ) ⬇️
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git commit -m "Auto-update data" || exit 0
          git push
